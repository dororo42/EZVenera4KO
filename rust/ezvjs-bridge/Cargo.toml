[package]
name = "ezvjs-bridge"
version = "0.1.0"
edition = "2021"
description = "QuickJS bridge for EZVenera KOReader plugin — C ABI shared library"
license = "GPL-3.0-only"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]

[build-dependencies]
cc = "1"