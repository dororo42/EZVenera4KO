# EZVenera_KO 双机部署 handoff 增补（2026-09-22 上午）

> 本文件是 `handoffs/2026-09-21-2103-ezv-dual-device-deploy/HANDOFF.md` 的**增补记录**，接手时两份一起读。以本份为最新状态。

## 昨夜至今晨的进展

### 1. 144 编译机：kindle5 工具链 ✅ 完成

- crosstool-ng 构建完成：`~/x-tools/arm-kindle5-linux-gnueabi/`（gcc 14.4.0，33 个二进制）
- 途中坑全部踩平，**复现时直接用现成脚本**：
  - tarball 下载极慢/损坏：脚本 `/tmp/fixtr.sh`（修正 tarballs 目录 + 重启 build）；缺哪个包用 `/tmp/fetch_exact.sh` 逻辑从 mirrors.tuna.tsinghua.edu.cn / zlib.net / invisible-mirror.net / ftp.gnu.org 补（**wget 断点续传的 .tmp-dl 文件常是坏的，gzip -t 校验后手工 mv**）
  - 已预置齐全的 tarball：binutils-2.43.1/2.44、gcc-13.4.0/14.4.0、glibc-2.12.2、gmp-6.2.1/6.3.0、isl、linux-2.6.32.71、make-4.3、mpc-1.3.1、mpfr、ncurses-6.4、zlib-1.3/1.3.1、zstd（共 18 个）
  - `clock_gettime` 链接错误只出现在 quickjs 的测试可执行文件（api-test/run-test262），**主产物 libqjs.so 编译链接成功**，忽略 make 的 Error 2 即可
- 编译监控：`bash /tmp/check6.sh`（读 /tmp/ct_build6.log）

### 2. K4 引擎重编 ✅ 产物已推送

- 流水线：`/tmp/build_k4.sh`（fetch v0.17.0 → cmake kindle softfp → libqjs.so）+ `/tmp/build_k4_shim.sh`（ezvbridge shim + patchelf NEEDED libqjs.so.0→libquickjs.so）
- 产物在 144: `~/ezv/EZVenera_KO/out/{libquickjs.so,libezvbridge.so}`，已拷回 Windows `C:\Users\Administrator\Downloads\EZVenera_KO\build\new-kindle-libs\`（libquickjs 436464B / libezvbridge 9836B），**已 scp 到 K4:/mnt/us/koreader/plugins/ezvenera.koplugin/lib/**（07:34 mtime 已确认）
- sha256（144 侧）：libquickjs 1863516170E7948A… libezvbridge 4543036D3AC9CF68…

### 3. quickjs-ng v0.17 符号修复 ✅ 代码已改（主仓+release）

- **方案 A 落地**（`fix_k4_symbols2.py` + `fix_k4_symbols3.py`）：
  - jshost.lua cdef：`JS_ToCString` → `JS_ToCStringLen2(ctx, size_t* plen, val, bool cesu8)`；`JS_NewCFunction` → `JS_NewCFunction2(ctx, fn, name, length, cproto, magic)`（签名来自 vendor quickjs.h L900/L1330，已人工核对）
  - `_evalRaw` 两处调用改 `JS_ToCStringLen2(self.ctx, nil, val, false)`
  - `missingSymbols` 清单同步为 `JS_ToCStringLen2`/`JS_NewCFunction2`
  - 测试桩 `tests/test_jshost_shim.lua` makeFakeQuickjs 同步改名
- 回归：**125/125 全绿 + syntax 16/16**（主仓）
- 新 jshost.lua 已 scp 到 K4（grep 确认 4 处 JS_ToCStringLen2 在位）
- release/ezvenera.koplugin/runtime/jshost.lua 同步修改

### 4. 平板 M2 扫码崩溃 ✅ 根因修复已落地并推送

- 用户提供的崩溃 report（Android HwResolver shared text.txt）+ 设备 logcat 实锤：
  `NativeThread: Failed to run script: remoteinput.koplugin/main.lua:74: attempt to index local 'socket' (a nil value)`
- 根因：fdroid KOReader（monolibtic）上 `pcall(require,"socket")` 返回 ok=true 但模块为 nil 的**假成功**，init 无守卫 → 点击菜单时 index nil 崩溃
- 修复（`fix_m2_socket.py`）：`if not socketok or type(socket) ~= "table"` 双守卫，src + release 同步；125/29 测试全绿
- 已 adb push 到 M2 并重启 KOReader（07:0x），inspector 确认 remoteinput 对象注册正常、无 NativeThread 报错复发
- **用户已决策：安卓扫码输入延后开发** —— remoteinput 在 M2 上保底不崩即可，端到端扫码后续再测
- 遗留：K4 上 remoteinput available()=false（扫码按钮不出现）——同为 socket 探测链路，等 K4 网络稳定后用同思路排查（crash.log 里 `pcall(require,"socket")` 在 K4 的真实结果）

## 当前卡点（唯一）

**K4 SSH 通道不稳定**：ping 通但 2222 端口 banner exchange 超时（08:41 起）。昨夜为验证引擎状态曾 pkill reader.lua 触发 KOReader 重启，USBNetwork 的 sshd 可能被 KOReader 重启过程拖垮或 K4 进入了 WiFi 省电。K4 屏幕应显示 KOReader 前屏。

**待执行动作（恢复 SSH 后）**：
1. `grep -cF JS_ToCStringLen2 /mnt/us/koreader/plugins/ezvenera.koplugin/runtime/jshost.lua`（应为 4）
2. 让用户在 K4：菜单 → EZVenera 漫画 → 浏览漫画源 → 观察「引擎不可用」是否消失（若出现 M2 浏览界面属 M2 里程碑提示 = **引擎修复成功**）
3. 菜单 → 引擎与状态 → 应显示「JS 引擎: 已安装」且不再翻转
4. 若仍失败，抓 `grep -F ezvenera /mnt/us/koreader/crash.log | tail -10` 看新的降级原因

## K4 SSH 恢复技巧（按序试）

1. K4 触屏/按键唤醒（KOReader 前屏操作一下即可恢复 WiFi）
2. KUAL → USBNetwork → Restart（若装了 KUAL 菜单）
3. 重启 K4（电源键）
4. 均无效则等 KOReader 空闲（省电策略恢复）

## 下一步总览（优先级序）

1. K4 SSH 恢复 → 验证引擎状态页（上面待执行动作）
2. 引擎 OK 后：K4 菜单 → 网络代理 → 编辑代理地址 → **确认「扫码远程输入」按钮是否出现**（available() 排查）
3. 平板 arm64 引擎（已授权方案 A）：NDK 已装 ~/android-ndk，复用 /tmp/build_k4.sh 思路换 `aarch64-linux-android21-clang` + `-DANDROID_ABI=arm64-v8a -DANDROID_PLATFORM=android-21`，产物推 M2（/sdcard/koreader/plugins/ezvenera.koplugin/lib/）——注意 M2 的 ezvenera.koplugin 整体还没推过（只有 remoteinput），需先推全套 Lua + 修好的 jshost.lua
4. 双机代理连通性实测（等用户提供代理地址）
5. 全部实测后出部署实测报告（HTML），追加 reports/

## 环境速查（增量）

- 144 工具链：`~/x-tools/arm-kindle5-linux-gnueabi/bin/arm-kindle5-linux-gnueabi-{gcc,readelf,strip}`；`patchelf` 系统已有
- 144 项目源码：`~/ezv/EZVenera_KO/{rust/ezvjs-bridge,scripts,shim,src}`（已同步 main.lua/jshost.lua 修复后版本需重新 scp）
- 144 产物：`~/ezv/EZVenera_KO/out/`
- Windows 产物镜像：`C:\Users\Administrator\Downloads\EZVenera_KO\build\new-kindle-libs\`
- M2 崩溃日志：设备 `/sdcard/koreader/.dbg_live.log`（常驻 logcat -f）；用户可从系统分享导出 "Android HwResolver shared text.txt"
- 主仓测试：`tools\.venv\Scripts\python.exe scripts\run_tests.py`（125/0 基线）
