# EZVenera KOReader — M2 里程碑进度（2026-09-22 18:2x）

## 已完成（代码落地 + 测试全绿）

| 任务 | 内容 | 测试 |
|---|---|---|
| T15 htmlparse.lua | HTML→DOM + CSS 选择器子集（tag/#id/.class/[attr=v]/后代/>/逗号分组），20 个桥契约方法全实现 | 11 项单测 |
| T16 sources.lua | 源仓库：官方索引+jsdelivr CDN 自动回退、本地 index 目录合并（key 冲突本地优先）、安装/更新/删除（sha256 记账+原子写）、本地 .js 直装；fetchUrl 统一走 netclient:request（per-request 代理） | 6 项单测 |
| T17 browser.lua | 浏览 UI 全层级：源列表→源主页（搜索+分类）→结果列表（翻页）→详情（章节分组）→ T18 阅读器调用；opds.koplugin Menu 范式 | 接线验证 |
| T18 阅读器 | opdspse 范式：onImageLoad config → loadEp 页表 → metatable 惰性下载 → RenderImage:renderImageData → ImageViewer fullscreen | 接线验证 |
| main.lua 接线 | 浏览漫画源→Browser、管理漫画源→「浏览索引并安装」+「已安装源（查看/删除）」完整交互（ConfirmBox 确认）、dispatcher EzVeneraOpen 同步接入 | — |

回归：syntax 19/19 · tests **143/0** · hold clean(19) · vendor SHA256 match

## 设备操作记录（用户指令执行）

- **M2 平板**：remoteinput.koplugin 已删除（扫码输入不需要）；ezvenera 保留最新全量（含 browser/sources/htmlparse）
- **K4**：ezvenera.koplugin + remoteinput.koplugin 均已删除（两插件暂停）；K4 KOReader 已重启恢复干净状态

## 真机验证状态

- EzVeneraOpen dispatcher → Browser:showSourceList →「没有已安装的漫画源」提示 ✅（弹窗实测截图确认）
- 引擎链路每次触发自动重跑：私有目录拷贝 → dlopen → shim install → JS init ✅（logcat 18:06 全链无错）
- 索引拉取（GitHub 直连）待人工在平板菜单实测（「管理漫画源 → 浏览索引并安装」）——直连可能超时，若失败需配置代理后重试

## 待人工/下轮

1. 平板人工：「管理漫画源 → 浏览索引并安装」→ 选源 → 安装 → 回「浏览漫画源」实测完整浏览/搜索/阅读流
2. 若索引拉取失败：先在「网络代理」配代理（或换 CDN 基址——代码已自动回退 jsdelivr）
3. T20/T21（modifyImage 像素管线、GBK 解码）→ M3
4. K4 恢复上线时：jshost/引擎产物已是最新，按 android 同链路复验

---

## 增补 2：2026-09-22 21:45 扫码功能移除 + 菜单 weread 式

- **扫码远程输入彻底移除**（用户最终决策）：proxyconf.lua 整段删除（src+release），
  tests/test_proxyconf.lua 的弱依赖用例保留（pcall 未装即跳过，不影响）。
  remoteinput.koplugin 本体保留在 src/release（历史代码），未推送任何设备。
- **菜单 weread 式重写**：menu_items.ezvenera 照抄 weread/ui/menu.lua:39-46 ——
  sorting_hint="tools" + sub_item_table_func 延迟构建 + gettext 包裹。
  此前直接 sub_item_table 在 Android 真机菜单位置异常（用户实测一直进书本区第 2 页）。
- **闪退定位**（拷贝漫画点击）：native SIGSEGV（21:05 am_crash），非 Lua 层。
  时间线：私有目录旧 SONAME libquickjs（12:31 版）+ 新 shim NEEDED libquickjs.so
  → Android 6 linker 混乱。修复已推：新 SONAME libquickjs + jshost rm 强刷新私有副本。
  **注意**：华为 force-stop/kill 均不可靠，重启必须上滑清后台再启动。
- 回归：syntax 19/19 · tests 143/0 · hold clean · vendor OK。
